// Re-export from the modular structure (mirrors the web app's provider API)
export { BookmarkProvider, useBookmarks } from './bookmarks';
export type { BookmarkContextType, SectionId } from './bookmarks';

